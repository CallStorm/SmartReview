# SmartReview 镜像推送配置（可选）
# 复制为 scripts\build.env 后按需修改。
# 注意：每行 KEY=VALUE，= 两侧不要加引号；# 开头的行为注释。
#
# 内网仓库（如 10.72.2.15:80）通常无需 docker login。
# 若使用外网仓库需登录：
#   docker login <REGISTRY>

# 镜像仓库地址（内网默认 10.72.2.15:80）
# Docker Hub           ：docker.io
# 阿里云 ACR           ：registry.cn-hangzhou.aliyuncs.com
# 腾讯云 TCR           ：ccr.ccs.tencentyun.com
# 自建 Harbor          ：harbor.your-company.com
# 内网                 ：10.72.2.15:80
REGISTRY=10.72.2.15:80

# 命名空间/用户名（注意末尾保留 / ）
# 内网仓库子路径       ：review/
# Docker Hub 用户名    ：yourname/
# 阿里云命名空间        ：your-namespace/
NAMESPACE=review/